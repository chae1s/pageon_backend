package com.pageon.backend.repository;

import com.pageon.backend.entity.ContentDelete;
import org.springframework.data.jpa.repository.JpaRepository;

public interface ContentDeleteRepository extends JpaRepository<ContentDelete, Long> {
}
